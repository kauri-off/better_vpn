# Better VPN backend environment. Copy to /etc/better_vpn/panel.env.
# Everything else is a runtime setting: edit it in the panel (Panel tab),
# in vpnctl, or with `vpn-backend --env-file <this file> set <key> <value>`.

DATABASE_URL=/var/lib/better_vpn/panel.db
RUST_LOG=info
